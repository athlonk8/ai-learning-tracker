# Agent instructions

This repository is for DataTalksClub AI Dev Tools Zoomcamp 2026 Homework 1.

- Read spec.md and backlog.md before changing code.
- Implement only the task requested by the user. The current authorized scope is Task 1; leave later tasks planned.
- Keep exactly the four product features in spec.md. Avoid speculative additions.
- Use Python, Django, uv, SQLite, and Django's built-in tests. Keep dependencies minimal.
- Use `config` for the Django project and `learning` for the app.
- Include migrations for model changes and meaningful tests for persistence and validation.
- Run `uv run python manage.py migrate`, `uv run python manage.py check`, `uv run python manage.py makemigrations --check --dry-run`, and `uv run python manage.py test` after implementation.
- Report actual commands and results. Never claim an unexecuted check passed.
- Update task status only after its acceptance criteria have been verified.
- Keep README commands current. Do not commit .venv, SQLite databases, or Python caches.
